apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: route
spec:
  hosts:
    - example.com
  http:
    - route:
        - destination:
            host: hello.default.svc.cluster.local
            port:
              number: 80
